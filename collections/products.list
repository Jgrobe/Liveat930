<section class="merch-landing">
    <div class="fill-parent mask">
        <div class="landing-bg" style="background-image:url('{collection.mainImage.assetUrl}');"></div>
    </div>
    <h1 class="l-headline">{collection.navigationTitle}</h1>
    <div class="cta-dummy" data-label="View my shopping cart" data-icon="icon-arrow" data-class="merch-landing-cta">{@|apply svg-icon-arrow.block}</div>
    <!--php print_cta(array(
        'label' => 'View my shopping cart',
    'icon' => 'images/icon-arrow',
    'class' => array('merch-landing-cta')
    )); -->
</section>

<section class="merch-overview">
    {.repeated section items}
        <div class="merch-item">
            <div class="image img vertical rel" style="background-image:url('{assetUrl}');">
                <div class="title">Live at 9:30 {title}</div>
                {.repeated section variants}
                    {.equal?:@index:1}
                        <div class="price">{@|product-price}</div>
                    {.end}
                {.end}
            </div>
            <div class="description">{excerpt}</div>
            <ul class="sizes">
                {.repeated section variants}
                    {.repeated section optionValues}
                        {.equal?:optionName:"Size"}
                            <li>{value}</li>
                        {.end}
                    {.end}
                {.end}
            </ul><!-- .sizes harro mada -->
            <!--div class="cta-dummy" data-label="Buy now!" data-class="cta-buy"></div-->
            {@|product-status}
            <!-- php print_cta(array(
            'label' => 'Buy now!',
            'class' => array('cta-buy')
            )); -->
            <!--<a href="" class="cta buy">Buy now!</a>-->
            {@|like-button}
        </div><!-- .merch-item -->
    {.end}
</section>

<?php include('modules/upcoming.php'); ?>